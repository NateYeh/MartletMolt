# TASK-ARCH-002: 沙盒容器化與物理隔離 (The Fortress)

## 1. 目標
將 Agent 執行環境從宿主機/主容器中隔離，利用 Docker 達成真正的物理沙盒化。

## 2. 詳細規格
- **Dockerfile 拆分**:
  - `docker/Dockerfile.host`: 包含管理邏輯、Orchestrator、以及與外部通訊的權限。
  - `docker/Dockerfile.sandbox`: 極簡環境，僅包含 Python 運行環境、Agent 程式碼與必要的工具套件。
- **網路隔離 (Networking)**:
  - 使用 `docker-compose` 建立內部網路。
  - Sandbox 容器不開啟外部對應埠，僅能透過 Host 代理對外。
- **目錄掛載優化**:
  - Sandbox 僅掛載 `skills/` 與 `workspace/`。
  - 禁止 Sandbox 掛載 `Config/` 目錄中包含 Secrets 的部分。

## 3. 修改路徑清單 (預計)
- `docker-compose.yml` (更新服務定義)
- `docker/Dockerfile.sandbox` (新增)
- `scripts/start_sandbox.sh` (新增)

## 4. 驗證方式
- 執行 `docker network inspect` 確認 Sandbox 網路路徑符合預期。
- 在 Sandbox 內嘗試 `curl google.com` 確認連線受限。
- 確認 Sandbox 只能存取對應的 `workspace/` 路徑，無權讀取 Host 檔案。

---
**狀態**: 📋 待啟動 (Planned)
**建立日期**: 2025-02-23
