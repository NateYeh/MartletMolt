# {{ config.metadata.title }}

> **版本**: v{{ config.metadata.version }}  
> **Base URL**: `{{ config.metadata.base_url }}`  
> **最後更新**: {{ config.metadata.last_updated }}

---

## 📋 目錄

- [概覽](#概覽)
- [快速開始](#快速開始)
- [API 端點總覽](#api-端點總覽)
- [詳細 API 文件](#詳細-api-文件)
{% for endpoint in endpoints %}
  - [{{ loop.index }}. {{ endpoint.title }}](#{{ endpoint.title | lower | replace(' ', '-') }})
{% endfor %}
- [錯誤處理](#錯誤處理)
- [JavaScript/TypeScript SDK](#javascripttypescript-sdk)
- [使用範例](#使用範例)
- [常見問題](#常見問題)

---

## 概覽

{{ config.overview }}

### 核心特性

{% for feature in config.features %}
- {{ feature.icon }} **{{ feature.title }}**: {{ feature.description }}
{% endfor %}

### 技術規格

- **框架**: {{ config.tech_specs.framework }}
- **數據格式**: {{ config.tech_specs.data_format }}
- **編碼**: {{ config.tech_specs.encoding }}
- **CORS**: {{ config.tech_specs.cors }}

---

## 快速開始

{% for step in config.quick_start %}
### {{ loop.index }}. {{ step.title }}

```bash
{{ step.command }}
```
{% endfor %}

---

## API 端點總覽

{{ api_endpoints_table }}

---

## 詳細 API 文件

{% for endpoint in endpoints %}

### {{ loop.index }}. {{ endpoint.title }}

**端點**: `{{ endpoint.endpoint.method }} {{ endpoint.endpoint.path }}`

**描述**: {{ endpoint.endpoint.description }}

**請求**:
{% if endpoint.endpoint.method == 'GET' or endpoint.endpoint.method == 'DELETE' %}
```http
{{ endpoint.endpoint.method }} {{ endpoint.endpoint.path }} HTTP/1.1
Host: localhost:8001
```
{% else %}
```http
{{ endpoint.endpoint.method }} {{ endpoint.endpoint.path }} HTTP/1.1
Host: localhost:8001
Content-Type: application/json

{{ endpoint.request.body | tojson(indent=2) }}
```
{% endif %}

{% if endpoint.request.parameters %}
**請求參數**:

| 參數 | 類型 | 必填 | 預設值 | 描述 |
|------|------|------|--------|------|
{% for param in endpoint.request.parameters %}
| `{{ param.name }}` | `{{ param.type }}` | {% if param.required %}✅{% else %}❌{% endif %} | {% if param.default %}`{{ param.default }}`{% else %}-{% endif %} | {{ param.description }} |
{% endfor %}
{% endif %}

{% if endpoint.request.path_parameters %}
**路徑參數**:

| 參數 | 類型 | 描述 |
|------|------|------|
{% for param in endpoint.request.path_parameters %}
| `{{ param.name }}` | `{{ param.type }}` | {{ param.description }} |
{% endfor %}
{% endif %}

**回應**:
{% if endpoint.response.format == 'Server-Sent Events (SSE)' %}
```
{{ endpoint.response.body }}
```
{% else %}
```json
{{ endpoint.response.body | tojson(indent=2) }}
```
{% endif %}

{% if endpoint.response.fields %}
**回應欄位**:

| 欄位 | 類型 | 描述 |
|------|------|------|
{% for field in endpoint.response.fields %}
| `{{ field.name }}` | `{{ field.type }}` | {{ field.description }} |
{% endfor %}
{% endif %}

{% if endpoint.status_codes %}
**狀態碼**:
{% for status in endpoint.status_codes %}
- `{{ status.code }} {{ status.description }}`: {{ status.description }}
{% endfor %}
{% endif %}

**範例**:
```javascript
{{ endpoint.examples.javascript }}
```

---
{% endfor %}

## 錯誤處理

### 錯誤回應格式

當 API 發生錯誤時，會返回 HTTP 錯誤狀態碼和以下格式的 JSON：

```json
{
  "detail": "錯誤描述訊息"
}
```

### 常見錯誤碼

| 狀態碼 | 描述 | 可能原因 |
|--------|------|----------|
{% for error in common_schemas.error_responses %}
| `{{ error.status_code }} {{ error.reason }}` | {{ error.description }} | {{ error.causes | join(', ') }} |
{% endfor %}

### 錯誤處理範例

```javascript
try {
  const response = await fetch('http://localhost:8001/chat', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ message: '你好' })
  });

  if (!response.ok) {
    const error = await response.json();
    console.error(`HTTP ${response.status}:`, error.detail);
    return;
  }

  const data = await response.json();
  console.log(data.message);
  
} catch (error) {
  console.error('網絡錯誤:', error.message);
}
```

---

## JavaScript/TypeScript SDK

{{ sdk.description }}

### SDK 檔案: `{{ sdk.filename }}`

```{{ sdk.language | lower }}
{{ sdk.sections[0].code }}

{{ sdk.sections[1].code }}
```

---

## 使用範例

### Python 範例

```python
import requests

# 初始化客戶端
BASE_URL = "http://localhost:8001"

# 健康檢查
response = requests.get(f"{BASE_URL}/health")
print(response.json())

# 同步對話
response = requests.post(
    f"{BASE_URL}/chat",
    json={"message": "你好", "session_id": "test-session"}
)
print(response.json())

# 串流對話
import sseclient  # pip install sseclient-py

response = requests.post(
    f"{BASE_URL}/chat/stream",
    json={"message": "寫一首詩", "session_id": "test-session"},
    stream=True
)

client = sseclient.SSEClient(response)
for event in client.events():
    if event.data == "[DONE]":
        break
    elif event.data.startswith("[ERROR]"):
        print(f"錯誤: {event.data[8:]}")
    else:
        print(event.data, end='', flush=True)
```

### Shell/cURL 範例

```bash
# 健康檢查
curl http://localhost:8001/health

# 系統狀態
curl http://localhost:8001/status

# 同步對話
curl -X POST http://localhost:8001/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好"}'

# 串流對話
curl -X POST http://localhost:8001/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"message": "寫一首詩"}'

# 列出會話
curl http://localhost:8001/sessions

# 取得會話詳情
curl http://localhost:8001/sessions/default

# 刪除會話
curl -X DELETE http://localhost:8001/sessions/old-session
```

---

## 常見問題

### Q: 如何保持對話上下文？

**A**: 使用 `session_id` 參數即可。相同的 `session_id` 會共享對話歷史：

```javascript
// 第一次對話
await client.chat('我叫小明', 'my-session');

// 第二次對話（會記住之前說過的名字）
await client.chat('我叫什麼名字？', 'my-session');
```

### Q: 串流對話如何處理錯誤？

**A**: 監聽 `[ERROR]` 標記：

```javascript
await client.chatStream(
  '問題',
  'session-id',
  (chunk) => console.log(chunk),
  (error) => console.error('錯誤:', error)
);
```

### Q: 支援哪些 AI Provider？

**A**: 目前支援：
- **OpenAI** (GPT-4o, GPT-3.5 Turbo 等)
- **Anthropic** (Claude 系列)
- **Ollama** (本地模型)

### Q: 如何設定超時時間？

**A**: 在 SDK 初始化時設定：

```javascript
const client = new MartletMoltClient('http://localhost:8001', 60000); // 60 秒超時
```

### Q: 會話資料儲存在哪裡？

**A**: 預設使用本地檔案系統儲存，未來將支援：
- Redis
- PostgreSQL
- SQLite

---

## 更多資源

- [GitHub Repository](https://github.com/your-org/martletmolt)
- [Issue Tracker](https://github.com/your-org/martletmolt/issues)
- [Contributing Guide](../CONTRIBUTING.md)

---

**最後更新**: {{ config.metadata.last_updated }}  
**文件版本**: v{{ config.metadata.version }}