---
# Pre-commit hooks 配置
# 完整的程式碼品質檢查，包含 Python 和 YAML

repos:
  # Python 程式碼格式化與檢查
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  # 型別檢查
  - repo: https://github.com/RobertCraigie/pyright-python
    rev: v1.1.383
    hooks:
      - id: pyright

  # YAML 檢查
  - repo: https://github.com/adrienverge/yamllint.git
    rev: v1.35.1
    hooks:
      - id: yamllint
        args: [-c, .yamllint]

  # 一般檔案檢查
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--unsafe]
      - id: check-json
      - id: check-toml
      - id: check-added-large-files
        args: [--maxkb=1000]
      - id: detect-private-key

  # 禁止提交敏感資訊
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: [--baseline, .secrets.baseline]

# CI 設定
ci:
  autofix_prs: true
  autofix_commit_msg: "🔧 auto-fix: pre-commit自動修正"
  autoupdate_branch: main
  autoupdate_commit_msg: "⬆️ auto-update: 更新pre-commit hooks"
