# MartletMolt 🦅

> A Self-Evolving AI Agent System with A/B Architecture

MartletMolt 是一個具有 **自我修改、自我重啟、自我進化** 能力的 AI Agent 系統。透過獨特的 A/B 雙系統架構，實現零停機的持續進化。

---

## ✨ 核心特點

| 特性 | 說明 |
|------|------|
| **🔄 Self-Evolving** | AI 可以修改自己的程式碼並完成升級 |
| **♻️ Zero-Downtime** | A/B 架構確保升級過程服務不中斷 |
| **🛡️ Auto-Rollback** | 進化失敗自動回滾，確保系統穩定 |
| **🕷️ Web Automation** | 基於 Playwright 的網頁自動化，無需截圖即可理解網頁 |
| **🔧 Extensible Tools** | 模組化的 Tool 系統，易於擴展 |

---

## 🏗️ 架構設計

### A/B 雙系統架構

```
┌─────────────────────────────────────────────────┐
│                  Orchestrator                    │
│        (獨立守護程序，管理 A/B 生命週期)           │
└─────────────────────────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        ▼                           ▼
┌───────────────┐           ┌───────────────┐
│   System A    │           │   System B    │
│   🟢 Running  │           │   ⚫ Inactive  │
│   (服務中)     │           │   (待命備份)   │
└───────────────┘           └───────────────┘
```

### 自我進化流程

```
運行中系統 A                  待命系統 B
    │                            │
    │  1. AI 決定需要進化         │
    │──────────────────────────► │
    │                            │
    │  2. 修改 B 系統程式碼       │
    │----------------------------│
    │                            │
    │  3. 啟動 B 系統測試         │
    │                       🟢 Running
    │                            │
    │  4. 健康檢查                │
    │◄──────────────────────────►│
    │                            │
    │  5a. 失敗 → 回滾 B，A 繼續  │
    │      ──────────────────────│──► ⚫ 停止
    │                            │
    │  5b. 成功 → 切換服務        │
    ⚫ 停止                      🟢 Running
    │                            │
    │  6. 同步 A ← B (備份)       │
    │◄───────────────────────────│
    │                            │
 ⚫ Inactive                   🟢 Running
 (備份)                       (服務中)
```

---

## 📁 目錄結構

```
MartletMolt/
├── orchestrator/                    # 🎯 核心調度器 (獨立運行，不可被 AI 修改)
│   ├── __init__.py
│   ├── manager.py                  # A/B 系統生命週期管理
│   ├── health_check.py            # 健康檢查
│   ├── switcher.py                # A/B 切換邏輯
│   ├── sync.py                    # 系統同步
│   └── config.py                  # Orchestrator 配置
│
├── system_a/                        # A 系統 (完整服務)
│   └── martlet_molt/
│       ├── __init__.py
│       ├── core/
│       │   ├── __init__.py
│       │   ├── agent.py           # AI Agent 核心
│       │   ├── session.py         # 會話管理
│       │   └── config.py          # 系統配置
│       ├── providers/
│       │   ├── __init__.py
│       │   ├── base.py            # Provider 抽象基類
│       │   ├── openai.py          # OpenAI Provider
│       │   ├── anthropic.py       # Anthropic Provider
│       │   └── ollama.py          # Ollama Provider
│       ├── tools/
│       │   ├── __init__.py
│       │   ├── base.py            # Tool 抽象基類
│       │   ├── web_navigate.py    # 網頁導航
│       │   ├── web_extract.py     # 內容提取
│       │   ├── web_click.py       # 點擊操作
│       │   ├── web_fill.py        # 表單填寫
│       │   ├── web_evaluate.py    # JS 執行
│       │   ├── web_screenshot.py  # 網頁截圖 (可選)
│       │   ├── shell.py           # Shell 命令
│       │   ├── file_read.py       # 檔案讀取
│       │   ├── file_write.py      # 檔案寫入
│       │   └── mysql.py           # 資料庫操作
│       ├── api/
│       │   ├── __init__.py
│       │   ├── routes.py          # REST API
│       │   └── websocket.py       # WebSocket (串流輸出)
│       ├── cli.py                 # CLI 入口
│       └── main.py                # 服務入口
│
├── system_b/                        # B 系統 (完整服務，與 A 結構相同)
│   └── martlet_molt/
│       └── ...
│
├── shared/                          # 共享資源
│   ├── config/                     # 共用配置
│   │   └── settings.yaml          # 主配置檔
│   ├── data/                       # 對話歷史、會話資料
│   └── logs/                       # 系統日誌
│
├── state/                           # 系統狀態
│   └── state.json                  # {active: "a", version: {...}}
│
├── docs/                            # 文件
│   ├── architecture.md             # 架構說明
│   ├── tools.md                    # Tools 文檔
│   └── api.md                      # API 文檔
│
├── orchestrator.py                  # Orchestrator 入口點
├── pyproject.toml                   # Python 專案配置
├── README.md                        # 專案說明
└── .gitignore                       # Git 忽略設定
```

---

## 🔧 技術選型

| 層級 | 技術 | 說明 |
|------|------|------|
| **後端框架** | FastAPI | 非同步、自動 OpenAPI 文檔 |
| **AI SDK** | httpx + OpenAI SDK | 支援串流輸出 |
| **網頁自動化** | Playwright (sync_api) | 支援 DOM/Accessibility Tree |
| **CLI** | Typer + Rich | 美觀的終端介面 |
| **配置管理** | Pydantic Settings | 型別安全 |
| **日誌** | Loguru | 簡潔強大 |
| **程序管理** | subprocess + psutil | A/B 系統生命週期 |

---

## 🚀 開發路線圖

### Phase 1 - MVP (最小可用版本)

- [ ] 專案結構搭建
- [ ] Orchestrator 核心框架
- [ ] System A/B 基礎架構
- [ ] Provider 抽象層 (OpenAI 支援)
- [ ] Tool 系統基礎
- [ ] 網頁自動化 Tools
- [ ] CLI 介面
- [ ] 健康檢查與自動回滾

### Phase 2 - 功能增強

- [ ] 更多 Provider (Anthropic, Ollama)
- [ ] 更多 Tools (Shell, File, MySQL)
- [ ] 多輪對話歷史管理
- [ ] 串流輸出 (SSE/WebSocket)
- [ ] REST API 服務模式

### Phase 3 - 進化能力

- [ ] 自我修改機制
- [ ] A/B 系統切換
- [ ] 程式碼同步
- [ ] 版本追蹤

### Phase 4 - 可選功能

- [ ] Web UI (React/Vue)
- [ ] 多用戶支援
- [ ] Plugin 系統

---

## 📖 設計原則

### Orchestrator 保護機制

Orchestrator 是系統的守護者，必須保持穩定：

1. **最小化設計** - 只負責生命週期管理，不包含業務邏輯
2. **不可修改** - AI Tools 無法修改 `/orchestrator/` 下的檔案
3. **獨立運行** - 作為獨立程序，不受 System A/B 影響
4. **安全邊界** - 需要特權權限才能修改 Orchestrator

### Tool 系統設計

每個 Tool 都有清晰的輸入/輸出定義：

```python
from abc import ABC, abstractmethod
from typing import Any
from pydantic import BaseModel

class ToolResult(BaseModel):
    success: bool
    data: Any = None
    error: str = ""

class BaseTool(ABC):
    name: str
    description: str
    
    @abstractmethod
    def execute(self, **kwargs) -> ToolResult:
        """執行 Tool"""
        pass
```

### 網頁理解策略

不依賴截圖，而是透過結構化資訊：

| 資訊類型 | 取得方式 | AI 理解方式 |
|----------|----------|-------------|
| DOM Tree | `page.content()` | HTML 結構分析 |
| Accessibility Tree | `page.accessibility.snapshot()` | 語義化表示 |
| 元素定位 | CSS Selector | 精確定位操作 |
| Console Log | `page.on("console")` | JavaScript 錯誤追蹤 |

---

## 🎯 專案名稱由來

**Martlet** (馬丁鳥) 是一種傳說中沒有腳的燕子，象徵永不停歇的追求。  
**Molt** (蛻變) 代表自我更新、不斷進化。

MartletMolt = 永不停歇的自我進化。

---

## 📄 License

MIT License

---

## 🤝 Contributing

歡迎提交 Issue 和 Pull Request！

---

> Built with ❤️ for the future of self-evolving AI systems.